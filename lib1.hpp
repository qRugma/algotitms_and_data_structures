#pragma once

template<typename T>
void swap(T&, T&);

//Печать массива произвольной длины
template<typename T>
void print(const T& mas, int len);

bool nextPerm(int *P, int len);

#include "lib1.tpp"
